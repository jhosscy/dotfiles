{
  "mcpServers": {
    "context-mode": {
      "command": "context-mode"
    },
    "replicate": {
      "command": "bunx",
      "args": ["-y", "replicate-mcp@latest"],
      "env": {
        "REPLICATE_API_TOKEN": "${REPLICATE_API_TOKEN}"
      }
    }
  }
}
